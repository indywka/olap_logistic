@model IEnumerable<OLAP.Models.Клиенты>

@{
    ViewBag.Title = "Управление клиентами";
}

<div class="but_div">
    @Html.ActionLink("Добавить клиента", " ", routeValues: null, htmlAttributes: new { @class = "btn mb1 black bg-gray", @role = "button" })
    @Html.ActionLink("Вернуться обратно", "Managment", routeValues: null, htmlAttributes: new { @class = "btn mb1 black bg-gray", @role = "button" })
</div>

<div class="table_div">
    <table rules="all" class="table_price">
        <caption>Просмотр данных по клиентам</caption>
        <tr>
            <th>
                Клиент №
            </th>
            <th>
                ФИО
            </th>
            <th>
                Адрес
            </th>
            <th>
                Номер телефона
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.КлиентID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ФИО)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Адрес)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Телефон)
                </td>
                <td>
                    @Html.ActionLink("Изменить", " ", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Детали", " ", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Удалить", " ", new { /* id=item.PrimaryKey */ })
                </td>
            </tr>
        }

    </table>
</div>